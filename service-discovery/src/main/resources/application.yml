# Core
server.port: 8761
spring.application.name: service-discovery

# Service Discovery
eureka:
  instance:
    instance-id: service-discovery
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka